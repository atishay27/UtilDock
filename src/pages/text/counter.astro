---
/* English keeps the unprefixed URL. The body lives in TextCounter.astro so the
   eight locales render from one source. */
import TextCounter from '../../components/pages/tools/TextCounter.astro';
---

<TextCounter locale="en" />
