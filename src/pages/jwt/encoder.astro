---
/* English keeps the unprefixed URL. The body lives in JwtEncoder.astro so the
   eight locales render from one source. */
import JwtEncoder from '../../components/pages/tools/JwtEncoder.astro';
---

<JwtEncoder locale="en" />
