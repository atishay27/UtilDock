---
/* English keeps the unprefixed URL. The body lives in JwtDecoder.astro so the
   eight locales render from one source. */
import JwtDecoder from '../../components/pages/tools/JwtDecoder.astro';
---

<JwtDecoder locale="en" />
