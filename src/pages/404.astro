---
/* English keeps the unprefixed URL it has always had. The body lives in
   NotFound.astro so the eight locales render from one source. */
import NotFound from '../components/pages/NotFound.astro';
---

<NotFound locale="en" />
