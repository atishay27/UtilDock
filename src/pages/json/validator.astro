---
/* English keeps the unprefixed URL. The body lives in
   Validator.astro so the eight locales render from one source. */
import Validator from '../../components/pages/tools/Validator.astro';
---

<Validator locale="en" />
